package com.cohortmgmt.model;

/**
 * Enum representing the type of user in the system.
 */
public enum UserType {
    /**
     * Represents a paid user.
     */
    PAID,
    
    /**
     * Represents a free user.
     */
    FREE
}
